# =============================================================================
# Hattz Empire v2.2 - Environment Variables Template
# Copy this file to .env and fill in your API keys
# Docker: docker-compose up -d
# =============================================================================

# -----------------------------------------------------------------------------
# LLM API Keys
# -----------------------------------------------------------------------------

# OpenAI (PM-Worker: GPT-5.2 Thinking)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-openai-api-key-here

# Google Gemini (Reviewer-Worker: Gemini 2.5 Flash)
# Get from: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=your-google-api-key-here

# Perplexity (Research 용도)
# Get from: https://www.perplexity.ai/settings/api
PERPLEXITY_API_KEY=pplx-your-perplexity-api-key-here

# -----------------------------------------------------------------------------
# Anthropic API Key
# -----------------------------------------------------------------------------
# IMPORTANT: Claude CLI 컨테이너들은 이 키를 사용하지 않음!
# 구독(Pro/Max)으로 CLI를 실행하려면 docker-compose에서 ANTHROPIC_API_KEY=""로 설정.
# 이 키는 Web 컨테이너에서 직접 API 호출할 때만 사용.
# Get from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-your-anthropic-api-key-here

# -----------------------------------------------------------------------------
# ngrok (고정 도메인)
# -----------------------------------------------------------------------------
NGROK_AUTHTOKEN=your-ngrok-token
NGROK_DOMAIN=your-domain.ngrok-free.app

# -----------------------------------------------------------------------------
# Database (MSSQL - 선택)
# -----------------------------------------------------------------------------
# For Docker: use host.docker.internal to connect to host machine's DB
DB_SERVER=host.docker.internal
DB_PORT=1433
DB_NAME=hattz_empire
DB_USER=sa
DB_PASSWORD=your-password

# -----------------------------------------------------------------------------
# Flask Configuration
# -----------------------------------------------------------------------------
FLASK_SECRET_KEY=your-random-secret-key-here
FLASK_ENV=production

# Admin credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-admin-password

# -----------------------------------------------------------------------------
# Worker Configuration (v2.2)
# -----------------------------------------------------------------------------
# 핑퐁 방지: 최대 재작업 횟수 (이후 CEO 개입)
MAX_REWORK_ROUNDS=2

# 폴링 간격 (초)
POLL_INTERVAL=2
